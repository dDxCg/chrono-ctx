sources:
  - path/to/docs
  - path/to/skills
  - path/to/workflows

/* This is an example configuration file for the context sources definition.
You can copy this file to config.yaml and modify it as needed. */